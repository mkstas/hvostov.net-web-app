import { Type } from '@/entities/types';
import { FC } from 'react';

interface Props {
  type: Type;
}

export const UpdateType: FC<Props> = ({ type }) => {
  return (
    <div>
      <button></button>
    </div>
  );
};
